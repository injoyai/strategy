# Project Memory

## Current baseline

- Go backend lives at the module path `github.com/injoyai/strategy` (`go.mod`); frontend lives in `web/` (React + TypeScript + Vite + Ant Design + ECharts, vitest for tests). Both are runnable; delivery branch is `m0-foundation`.
- M0 progress: M0-01..M0-05 delivered and green under `scripts\verify.ps1`:
  - M0-01 skeleton/config/logging/graceful shutdown; M0-02 domain primitives and error model; M0-03 HTTP contract shell (auth, request ID, idempotency, pagination, error mapping, OpenAPI contract tests, TS client).
  - M0-04 metadata/migrations/artifacts: `internal/store` (SQLite + goose migrations with checksum tamper-detection), `idempotency_records`/`jobs`/`job_events`/`runs`/`provider_connection_versions`/`batches`/`snapshots`/`snapshot_batches`/`artifacts` tables in migration 00001; `result_refs` column in 00002. `internal/store/idempotency.go` SQLite store replaces the in-memory shim. `internal/artifacts` implements the content-addressed file protocol (staging→rename→metadata-tx) with `VerifyReferencedFiles` at startup and `Sweep` quarantine.
  - M0-05 job lifecycle: `internal/jobs` state machine (queued→running→succeeded, queued→cancelled, running→failed, running→cancel_requested→cancelled|failed), single-tx create (run+job+first event), lease/fencing-token claims, event stream bounded by `defaultEventWindow=1000` with 410 on expired Last-Event-ID, `Hub` fan-out, `RecoverOrphans` at startup. Wired into `cmd/researchd/main.go` and `internal/server/jobs.go` (GET/POST /jobs routes + SSE).
- M0-06 delivered (including slice F import boundary): `internal/synthetic` provider adapter, quality engine, `internal/data` DataStore + connections, 13 data-plane HTTP endpoints (`internal/server/data.go`), and `internal/pipeline` handlers wired into `jobs.Loop.Handlers` in main (kinds `connection.check`/`snapshot.publish`/`ingestion.run`/`import.validate`, constants duplicated in server and pipeline). Pipeline conventions: job config is the marshaled request stored in the run's immutable_config (read via new `jobs.Store.Config`, decoded with DisallowUnknownFields); ingestion re-checks the pinned connection version at run time (`resource.version_mismatch` — HEAD may move between API validation and worker execution), resolves Fields/frequency/page size from `Describe` capabilities (`FetchRequest.Validate` requires non-empty fields/instrument_ids/frequency), bar ingestion pre-fetches the calendar inside req.Range to feed the trading-day quality rule (calendar normalizer issues discarded), writes raw page evidence to `batches.raw_manifest` via new `ports.BatchInput.RawManifest` (JSON array of {dataset, content_type, checksum, bytes, fetched_at, next_cursor, provider_revision}), and SUCCEEDS even with error-severity issues — severity blocking happens only at snapshot publish (fail-closed), matching the M0-08 demo path. Connection check probes HEAD (no pin enforcement) and fails the job on error-severity issues. Test suites delivered and green: synthetic 36, data 14, pipeline 12 test funcs / 16 scenarios (incl. clean + bad-data e2e through jobs.Loop, version mismatch, fault gate exact error, fail-closed snapshot, instrument dataset); full-repo `go build`/`go vet`/`gofmt`/`go test ./...` pass.
- M0-07 delivered by parallel agent (commits b72f4cb/0afc2d4/d6fad74), verified in review: `npm run typecheck` clean + `vitest run` 3 files / 13 tests green. Layout deviates from frontend-delivery.md's suggested folders: `web/src/app/` (AppShell + `pages/{Connections,Data,Jobs}Page.tsx` + shared `ui.tsx` components), `web/src/api/` (`schema.d.ts` generated + `runtime.ts` request-id/idempotency/error-normalize + `client.ts` + `hooks.ts` react-query layer + `sse.ts`), `web/src/theme/tokens.ts`. Web conventions: SSE is a fetch stream (not EventSource) sending `Last-Event-ID`, resetting the cursor on 410 and reconnecting with exponential backoff (500ms*2^n capped 8s); polling fallback every 2.5s only while a job is active; all writes carry `Idempotency-Key`; selection/search live in URL search params; SearchField debounces 300ms and suppresses during CJK IME composition; job state labels mirror backend states; planned routes (M1/M2/M2R) render as explicitly gated, non-clickable nav items with a real 404 page.
- M0-08 delivered: `TestM0VerticalSliceAcceptance` (internal/server/m0_acceptance_test.go) walks the full 7-step demo path through the real HTTP mux + jobs.Loop: providers → idempotent connection create → ingestion 202+Location → SSE replay + Last-Event-ID resume → batches (8 rows / 2 warnings) → injected upstream fault → retry as a new job → strict-PIT snapshot publish → as_of graded visibility (0 → 2 → 8 items) → restart persistence (resources, job terminal state, SSE replay). Acceptance surfaced 4 contract-breaking production bugs invisible to prior tests, all fixed with evidence-based root causes (see dispatch/Flusher/cursor/availability-policy conventions below) plus `domain.Interval` JSON tags (`from`/`to` per openapi). `scripts\verify.ps1` fully green (Go + web 13 tests).
- M0-06 slice F delivered (TDD: 7 contract tests RED→GREEN, `internal/server/m0_import_test.go`): `POST /imports` (multipart, exactly `file`+`format`, unknown field → `validation.unknown_field`; media types csv→`text/csv`, parquet→`application/x-parquet`) records the upload artifact synchronously then queues `import.validate`; `GET /artifacts/{id}` and `/content` (Content-Type mirrors `artifact.media_type` exactly, `Content-Disposition: attachment`). `artifacts.Store` now owns the db handle (`New(root, db)`) with `Record`/`Get`/`Open`: Record upserts on `UNIQUE(storage_key)` so identical bytes always resolve to the same artifact id, and stamps `published_at = created_at` (operational metadata, no PIT semantics); migration 00004 adds the openapi-required `artifacts.name` column. `import.validate` re-verifies size+sha256 from disk before probing; CSV probe via `encoding/csv` SUCCEEDS with per-row errors (`row` = `csv.ParseError.Line`, 1-based counting the header; message `expected N columns, got M`) and fails on an empty file; parquet fails the job with an explicit "no approved parquet reader dependency" message — per m0-foundation.md the M0 boundary is upload/checksum/schema-probe/error-report only, not full import. Report JSON is stored as an `application/json` artifact named `import-report` and published as the job's single `artifact` ResultRef.
- Multipart idempotency gotcha: the idempotency middleware hashes RAW request bytes, so a multipart retry only replays if the client resends byte-identical bodies — `multipart.Writer` randomizes the boundary per instance, which turns a logical retry into a 409 `idempotency.conflict`. The acceptance test helper therefore pins a fixed boundary and sorts field names.
- DEC-03 approved: single-machine experiment deployment with SQLite via `modernc.org/sqlite` (pure Go, WAL); database must stay on a local filesystem.
- `docs/contracts/contracts.go` is a proposed extension-port draft, not a persistence model or runnable implementation. OpenAPI (`docs/api/openapi.json`, generated by `docs/api/build_openapi.py`) is the authoritative HTTP DTO contract; both stay in sync via `scripts\verify.ps1`.

## Architecture invariants

- Research inputs are immutable and versioned: Snapshot, UniverseVersion, FactorVersion, StrategyVersion and model policies. Job lifecycle is separate from Run identity and evidence.
- `DataView` is bound to `snapshot_id + as_of`; PIT selection uses `available_at`, preserves revisions, and must not substitute `ingested_at` (ReplayTime is only an optional secondary upper bound `ingested_at <= replay_time` for manual replay). The SQL also applies the half-open effective window `effective_from <= ? AND (? < effective_to)` (windowless rows always pass); as_of is pinned at OpenView — changing as_of requires reopening the view. Unknown PIT evidence fails closed in strict mode.
- Data plane conventions (internal/data): `quality_status` vocabulary is `clean`/`info`/`warning`/`error` (worst-severity reduction of the stored Issue list; error short-circuits). Publishing is fail-closed: any error-severity issue → `resource.conflict`, and strict PIT additionally rejects rows with `published_at IS NULL`. List APIs (batches/snapshots) do NOT mint cursors — the server's pagination layer decodes the 24h cursor and hands `Sort`/`AfterID`/`Limit` to the store, which returns the last item's id as `NextCursor` (id-only keyset); `/data/query` cursors are store-minted decimal offsets (safe: pinned snapshot+as_of is immutable). Observations carry denormalized query columns (available_at/revision_id/published_at) beside `provenance_json` as source of truth; PIT filter (`available_at <= as_of`) is applied in SQL BEFORE the latest-revision rule, whose winner is the last row under `ORDER BY instrument_id, event_time, available_at, revision_id, id` (rowid tie-break). Batch checksum = SHA-256 over sorted canonical row JSON; byte-identical rows within one Append collapse (cross-request retry dedup is the idempotency layer's job).
- Forced ingestion always calls the upstream provider and records a new request/batch even when cache is current; identical content may be checksum-deduplicated.
- Strategy code cannot access network, database, filesystem or unmanaged time. Backtests use fixed event ordering and seed. Ledger is the sole writer for cash, positions and equity.
- Unknown market, provider, account, deployment, fee, fill or metric decisions remain explicit blockers; do not create example defaults.

## Delivery plan

- `docs/implementation/` is the executable implementation baseline: M0 persistent Job/synthetic snapshot slice, M1 real data/PIT/universe/factors, M1S stock screening (`m1-screening.md`), M2 deterministic backtest/report/experiment loop, M2R manual historical replay (`m2-manual-replay.md`), M3 research enhancement, and the design gate that keeps M4 trading integration blocked, plus frontend and verification traceability.
- Screening S1 pure rules may start with synthetic inputs after shared contract semantics are reconciled; full ScreenRun requires M1 DataView/Universe/Factor. Replay RP-01 state/revision can start with synthetic inputs, while real order/advance work requires approved market/account/reservation/valuation policies and the shared M2 fill/cost/ledger core.
- M1S S1-01..S1-04 delivered (commit b0db66a): `internal/screening` pure-rules engine (definition/codes/validate/truth/ranking/engine, no DB/network/clock). Domain flags to reuse: `domain.Decimal` has no `String()` and no public Cmp — compare via `Sub` sign; a public `Div` exists since M1-07 (explicit zero-divisor error, never panics); weight-validity = `IsZero||IsNegative||IsPositive`; percentile via `math/big` round-half-up scaled by 10^Places (avg-rank numerator first+last-2, denominator 2(m-1); m=1→0.5). Engine semantics pinned: `input` is a `ScreenInput{binding_id}` object, condition 9 oneOf branches, 5 mutually-exclusive stages, rankable-official-first then excluded sorted by instrument ASC (shuffle oracle). fail_run fires only on condition-unknown or rank-insufficient (never display-only missing).
- M2R RP-00 delivered (commit ba94035): REPLAY-01..10 / RP-AC-01..14 entered `docs/requirements.md`; the Replay OpenAPI contract added to `build_openapi.py` (ReplayConfig/Preflight, ReplaySession, SessionCommand, ManualOrder/Create/ManualOrderCancel, ReplayStep, ReplayDataQuery, ReplayScreenRunCreate, AccountSnapshot/PositionRow, ReplayNote/Create, ReplayEvent, ReplayReport, ReplayExportCreate + 16 `/replay-sessions*` endpoints). Contract is fail-closed: market/fill/cost/valuation/metrics policies are `ModelBinding` refs, day_end/cash/end/order_types are explicit validated strings, no implicit defaults; every mutating command embeds `SessionCommand{command_id, expected_revision}`; read-only POSTs (`/replay-sessions/preflight`, `.../data/query`) are excluded from the Idempotency-Key list. Generated openapi: 69 paths / 78 ops / 121 schemas.
- M2R RP-01 delivered (`internal/replay`): pure session/command/history-clock state machine (definition/clock/codes/apply, no DB/network/unmanaged clock). Pinned semantics: `Init` derives the start decision point from a server `DecisionClock` (`AtOrAfter(cfg.From)`); every mutating op is a pure function returning a new `Session` (no in-place mutation), so two-tab competition is proven by applying the stale command against the freshest session (expected_revision vs `s.Revision` → `replay.revision_conflict`). Command carries `command_id` + `expected_revision` but NO historical time. State machine: `initializing→awaiting_action`; `awaiting_action→advancing→awaiting_action` (orders/cancel/advance rejected while advancing → `replay.state_invalid`); `awaiting_action→closing→completed`; non-terminal→`failed`. Advance is two-phase: `OpenAdvance` (→advancing, computes server next day; as_of unchanged) then `CommitAdvance` (→awaiting_action, publishes as_of + revision++); orders keep decision_at = current as_of. Config is fail-closed: market/fill/cost/valuation/metrics are `ModelRef{Ref VersionRef, Params}` validated (empty/invalid rejected); `Config.Hash()` is a canonical deterministic serialization (note: `domain.Decimal` has no `String()` — cast `string(amount)`). 12 test funcs green; full `go build/vet/test ./...` + contract chain + web build/test 13 green.
- M1-05 synthetic subset delivered: migration 00005 adds observations.ingested_at (ns, backfilled from provenance_json $.ingested_at, idx_obs_ingested; Down drops index before column); Append validates zero ingested_at and persists it; PublishSnapshot is idempotent — canonical manifest = {strict_pit, entries sorted by BatchID string}, hash via s.sum.Checksum, dedupe by (workspace, manifest_hash) returns the existing snapshot via GetSnapshot (BatchIDs keep first-publish ordinal, name/CreatedAt preserved); NO unique index on (workspace, manifest_hash) by design (pre-existing duplicates would break migration). DataView gains the effective-window filter plus conditional replay_time bound; domain.DataQuery.ReplayTime rejects zero in Validate; OpenAPI DataQuery.replay_time is nullable/optional (69 paths / 78 ops / 121 schemas regenerated). Append sorts rows within a batch (rowLess), so insertion-order-independence tests must use cross-batch ordering to prove the ORDER BY tie-break. 7 new tests (idempotent publish + strict_pit hash flip, effective window, future revision invisible, revision tie-break without supersedes, ns boundary precision, replay_time upper bound incl. zero rejection, zero ingested_at validation); gofmt/build/vet/test + contract chain + web typecheck green.
- M1-06 UniverseVersion delivered on top of M1-05: migration 00006 `universe_versions` (UNIQUE(workspace,name), snapshot_id, kind, definition_json, definition_hash). `domain.UniverseDefinition` is static (CanonicalMembers = sorted+deduped via in-file insertion sort — `internal/domain` has a closed import allowlist enforced by `architecture_test.go`, adding "sort"/"slices" is an ADR-level decision) or historical_rule ({dataset, frequency}); DefinitionHash covers only the canonical definition (name/snapshot binding excluded). CreateUniverseVersion is deliberately NOT idempotent (each call mints a new version); snapshot binding is fail-closed — ResolveUniverse requires the DataView's SnapshotID to equal the version's snapshot_id (`resource.conflict`, "is bound to snapshot"). historical_rule resolves through `ports.DataView.DatasetInstruments(dataset, frequency)` (new method: DISTINCT instrument_id, available_at<=as_of, half-open effective window, `instrument_id <> ''` so entity-only rows never leak); membership is state data with no event-time filter, so shrinking corrections are forever invisible while lengthening ones yield a legal member→knowledge-gap→corrected sequence. UniverseCacheKey = checksum over {resolver_version, snapshot manifest hash, universe id, definition hash, as_of}. 8 test funcs / 33 subtests green. Gotcha: `domain.Error.Error()` returns only Message — a Wrap's cause text never surfaces, so tests must assert the outer wrapper message (e.g. "universe: rule invalid").
- M1-07 factor registry/DAG/preflight/cache delivered: `internal/factor` (9 files, in-memory registry mirroring screening — no migration/store CRUD) + `domain.Decimal.Div` (explicit zero-divisor check returning `validation.invalid` "decimal: division by zero"; shopspring Div would panic otherwise; 16-digit quotient). Builtin (`Compute` closure) and restricted-expression factors (AST whitelist `+ - * / neg abs/log/sqrt/min/max`, depth 32, unit propagation: `* /` produce dimensionless, `+ -` require agreement) share one registry; deps must already be registered (the public API cannot construct a cycle — cycle tests seed `registry.specs` directly). Run order: validateRequest (Range.To must equal the view's as_of) → one-pass preflight returning ALL problems (params/schema/empty universe/per-input dataset_missing/field_missing/insufficient_history/pit_unverified/frequency_unsupported; the error inlines the full list into the Message because `domain.Error` renders only Message) → CacheKey hit → frameFor recursion (memo dedup; dep params pass through by name via `intersectParams`, so composite factors must mirror window params like reversal's n) → completeness gate → Stage→Publish (Publish failure Aborts). CacheKey = DefinitionHash (declarative JSON; Compute/LookbackFor function fields invisible — covered by `RuntimeVersion = "factor-runtime/1"`, bump on any Go-semantics change) + snapshot/universe/params canonical (explicit default == omitted)/range/frequency (single|""|mixed)/availability policy. Per-member missing reasons (missing_input/insufficient_history/stale_input/invalid_denominator/pit_unverified/not_applicable) are distinct from preflight problem codes; PIT rows (nil published_at or `pit_unverified` flag) are excluded from usable points; zero-lookback factors turn member-without-rows into missing_input instead of a failed preflight. Gotcha: `domain.Decimal` is string-backed — write `domain.Decimal("")`, never `domain.Decimal{}` (invalid composite literal), and arithmetic helpers return `(Decimal, error)` so evaluator sites wrap via a helper. 13 test funcs green (AC-05 both sides: PE factor on a bar-only snapshot fails with dataset_missing naming valuation while momentum computes 0.2 standalone); full gofmt/build/vet/test green.
- M1-08 factor analysis artifact delivered: `internal/analysis` (8 source files, pure package — no DB/network/wall clock; label isolation enforced structurally by `architecture_test.go`'s closed import allowlist: domain/factor/ports + encoding/json/math/sort/strconv/time only, test-only errors/strings/testing). Labels are the ONLY future information and enter exclusively via explicit `LabelSet{Horizon, Returns map[time.Time]map[domain.ID]float64}` (§6.2) — the caller computes them; Factor/Strategy DataView expose no label reads. `Engine.Run(Request{Config, Series []CrossSection{Time, *factor.Frame}, Labels})` → `*Artifact` (schema `analysis-artifact/1`): full per-date series (chart downsampling is render-time only, never mutates the artifact) + page summary + fixed config (ref, canonical params, DefinitionHash, label economics strings, groups/min-samples/method/range/segments) + EvidenceNote + SHA-256 checksum over canonical core JSON (`Artifact.VerifyChecksum`). Daily: covered/total, per-reason missing counts (frame reasons + analysis-added `invalid_value` for undecodable decimals — coverage per reason, never zeroed), interpolated-quantile distribution, per-horizon {pairs, IC Pearson, RankIC Spearman average-ranks, quantile buckets ordered by (value, instrument_id) with bucket 1 = lowest and sizes differing by ≤1, high-low spread}. Summary: pooled distribution/missing reasons, coverage rate, per-horizon decay block (IC/RankIC/`Correlation`=Config.Method-selected series aggregate, bucket mean-of-means, spread series; `SeriesSummary` mean/std/IR/positive_rate), train/validation/test segment accounting (dates/samples/per-horizon label counts) + uncovered dates. Null+reason contract via `Stat{*float64, Reason}` — reasons insufficient_samples/zero_variance/not_applicable/missing_labels/invalid_value; `valueStat` guards finiteness so NaN/±Inf can structurally never reach JSON. Validation fail-closed (series: ref match, Time==Frame.AsOf, unique in-range dates, member not in both frame maps; labels: horizon>0, key==Horizon, UTC-normalized dates with same-instant collision rejection, in-range, finite values); factor-side errors pass through with their factor.* codes because `domain.Wrap` drops cause text on the wire. Floating-point gotchas: a "constant" IC series is only truly constant with binary-exact values (labels like 0.1/0.2 make ICs differ at ~1e-17 → std tiny → IR explodes); n=2 sample std IS computable. Two factor-package additions: `Registry.CanonicalParams(ref, supplied)` (fail-closed canonicalization for artifact freezing) and `FactorRef` JSON tags id/version. 13 analysis test funcs green (byte-identical determinism under shuffled input order, tamper detection, JSON no-NaN, 25-case validation matrix, hand-computed quantiles/Spearman ties/buckets) + factor `TestCanonicalParams`; full gofmt/build/vet/test green.
- M1-09 后端 API 闭环 delivered: `internal/research` 编排层 + `server/universes.go` + `server/factors.go` + `server/api.go` 装配 + `server/errors.go` 状态码映射。Research service 暴露 `RunFactor`（同步横截面计算，preflight→frame→Stage/Publish→返回 Frame + Artifact ref）与 `RunAnalysis`（决策日推导→逐日 RunFactor→固定标签策略 `loadBarSeries.labelSets` 用 close(d+h)/close(d)−1 在 research 包内计算并经 `analysis.LabelSet` 显式注入→`analysis.Engine.Run`→`Artifact.Store` 密封并写库，相同输入字节级一致）。`internal/analysis` 通过 import 白名单继续禁止访问数据层，确保标签隔离。HTTP API：universes CRUD/resolve（resolve 用 `ports.DataView.Instruments` 在 as_of 预览成员）、factors list/detail、factor-runs 同步 preflight/run、factor-analyses 全流程；错误码 `factor.request_invalid`→400 / `factor.not_registered`→404 / `factor.preflight_failed`→422，`analysis.*` 同套语义。OpenAPI 契约 `build_openapi.py` 同步修正：`UniverseDefinition.members`（而非 instrument_ids）、`Factor` 与 M1-07 实现对齐、新增 `FactorAnalysisResult`/`AnalysisConfig`/`AnalysisSummary` schema 并移除过期 `FactorRunCreate` 等提案；契约现在 68 paths / 76 ops / 134 schemas。测试：research 全栈（成功路径/无预热历史/未注册因子/snapshot 不匹配/as_of 错/确定性 byte-identical）+ server HTTP 闭环（200 成功/404 未注册/422 预检失败/Idempotency-Key 重试返回原结果）；full gofmt/build/vet/test + contract chain + web gen:api/typecheck/build + 13 vitest 全绿。下一会话：M1-09 前端页面闭环（成功/失败/重试）。
- M1-09 前端页面闭环 delivered: `web/src/app/pages/UniversesPage.tsx`（版本列表 + 新建 static/historical_rule + 成员预览）+ `web/src/app/pages/FactorsPage.tsx`（因子目录 + `/factors/:id` 详情 + 同步 preflight/run + 因子证据分析）+ `web/src/api/hooks.ts` 新增 universes/factors/factor-runs/factor-analyses 的 query/mutation + `AppShell` 路由与导航（`/universes`、`/factors`、`/factors/:id`，从 plannedItems 移出，页眉页脚 M0→M1）。按 frontend-delivery.md §3.1，选中版本放 URL：`/factors/:id?version=…`，目录与依赖均为 `<Link>`（依赖回链），`factorDetailHref` 保留当前 `q`。失败路径渲染 contract `code`/`request_id` 并可重放**同一**请求（`lastRequest` state，非重读表单）；`launch()` 切换动作时 reset 另一 mutation 以免陈旧错误与新结果同屏。两个必须记住的测试/运行时坑：(1) `openapi-fetch` 在 `createClient()` 时捕获 `globalThis.fetch`，而 `src/api/runtime.ts` 在 import 期就建好单例，所以 `beforeEach` 里的 `vi.stubGlobal("fetch")` 永远无效（既有 App.test.tsx 的 stub 实际一直没生效，其断言恰好不依赖数据才没暴露）；正确做法是 `vi.hoisted` + `vi.mock("../../api/runtime")` 用注入的 fetch 重建客户端。(2) 生产默认 `baseUrl:"/api/v1"` 是相对 URL，Node 的 `Request` 直接 reject，jsdom 里所有请求都在调用 fetch 前就抛错 —— 测试必须用绝对 baseUrl，故 `CreateApiClientOptions` 新增可选 `fetch` 注入点（openapi-fetch 原生支持）。另外 Ant Design 会在两字中文按钮里插入空格，`getByRole("button", { name: "预检" })` 找不到，需用 `/预\s*检/`。测试：18 vitest（新增 FactorsPage 5 例覆盖成功/预检失败/422+重试/503/URL 详情+依赖回链）+ App 路由与 title；`npm run typecheck`/`build` 与 `go test ./...`、契约链（openapi 未变，gen:api 零 diff）全绿。
- `scripts/verify.ps1` 有既有脆弱点：`$ErrorActionPreference='Stop'` 会把**原生命令写到 stderr 的普通警告**当成终止错误 —— `git diff`（CRLF will be replaced by LF）和 `vite build`（chunk size 警告）都会让脚本非零退出，但各步骤实际都通过。核查时按步骤单独运行确认，不要据此判定失败。
- SCREEN-01..08/SC-AC-01..12（c0ed81d）与 REPLAY-01..10/RP-AC-01..14（ba94035）均已进入主需求与 OpenAPI；对应的 Go/TypeScript DTO、migrations 与契约测试在两个功能的后续阶段分批实现。
- Multi-agent coding is governed by `docs/implementation/multi-agent-delivery.md`: use work-package-level Work Orders on an explicit base commit and separate branches/worktrees; serialize ownership of OpenAPI/generated clients, migrations, shared domain/ports, dependencies, global wiring, verification scripts, README, and MEMORY. Only the Integrator marks cross-package gates Verified after merged-base checks.
- M0 may proceed without choosing a market, but Go module path/toolchain and at least an M0 deployment assumption must be approved first. M1 requires DEC-01/02/03; M2 requires DEC-05/06 and versioned market/fill/cost/metric policies.
- Requirement/acceptance coverage tracks the full ID tables in `docs/requirements.md` (DATA/FACTOR/SCREEN/STRAT/BT/IMP/MANAGEMENT + SC-AC/RP-AC); stage completion requires code, tests, migrations/config, contract/docs and reproducible evidence.

## Contract chain conventions

- `docs/api/openapi.json` is generated by `docs/api/build_openapi.py` and `web/src/api/schema.d.ts` by `npm run gen:api`; both generated artifacts are committed and `scripts\verify.ps1` fails on any uncommitted drift. Never hand-edit them.
- Server routes are registered through `server.API.Handle` with patterns relative to the API root (e.g. `/things`); the `/api/v1` prefix is added automatically and matches the OpenAPI `servers` entry. Unmatched paths under `/api/v1` answer the contract error envelope (404 `resource.not_found`); 405 keeps ServeMux's default text because the spec declares no 405 responses.
- Contract behavior is pinned by `internal/server/contract_test.go`, which walks every operation in openapi.json (45 paths / 50 operations) with stub handlers; adding an operation to the spec automatically adds a test case.
- Error mapping is single-point in `internal/server/errors.go`: validation.*→400, auth.unauthorized→401, auth.forbidden→403, resource.not_found→404, resource.*conflict/idempotency.conflict→409, pagination.cursor_expired→422, rate.limited→429 (retryable), internal.unavailable→503 (retryable), anything else→500 with a fixed "internal error" message (fail-closed).
- Idempotency: required for POST operations declaring the `Idempotency-Key` header (8..128 chars); scope is `caller|workspace|method|path`; replays 24h then re-execute; store is in-memory for M0-03 — M0-04 adds the SQLite `idempotency_records` store plus goose migrations.
- Pagination: `limit` 1..200 (default 50); cursor is a base64url-encoded payload expiring after 24h (422 `pagination.cursor_expired`, strict `>` comparison at the TTL boundary); `sort` is whitelisted per endpoint (`id`/`-id`). An absent/empty `cursor` query param is the canonical first page and must short-circuit in `ParseCursor` — only non-empty malformed cursors answer 400.
- Server dispatch: `internal/server/api.go` Handler() must route through `mux.ServeHTTP(w, r)` (with a miss-check via `mux.Handler(r)` for the error-envelope 404). Manually invoking the handler returned by `mux.Handler(r)` never populates `r.PathValue` for `{id}` routes — the stdlib sets pattern/matches only inside `ServeMux.ServeHTTP`, so every wildcard-route fetch would 404 silently.
- Middleware ResponseWriter wrappers must forward `http.Flusher.Flush()`: `statusRecorder` (access-log) does, and SSE streaming silently deadlocks in net/http's 4KB response buffer without it (server goroutine healthy, client never sees headers).
- Availability policy: `pipeline.applyAvailabilityPolicy` stamps `Provenance.PublishedAt = &AvailableAt` when the upstream announces no publish time (conservative default per the domain.Provenance contract, never the event time) before Append; strict-PIT snapshot publish rejects batches whose observations carry NULL `published_at`, so skipping this makes strict-PIT snapshots unpublishable for such sources.

## Stock screening design scope

- The user scoped stock screening to design documentation only. `docs/stock-screening-design.md` is Proposed; its candidate endpoints, domain objects and SCREEN/SC-AC IDs are not active OpenAPI contracts or implemented features. Implementation must explicitly integrate the contract/client/test chain rather than treating the design examples as current API support.
- The design separates immutable ScreenerVersion rules from point-in-time ScreenRun results, reusing Snapshot/DataView, Universe/Factor, Job and Artifact boundaries. A saved static universe retains selection time and source evidence; it must not be backfilled into earlier backtest decisions. Unknown values use three-valued filtering; ranking membership, missing-value policy and tie-breaking are explicit.

## Historical replay direction

- The user explicitly chose manual historical replay: advance one trading day at a time and decide buys/sells in the UI. `docs/historical-replay-trading-design.md` records the Proposed design; automatic batch backtesting is a separate workflow, and this delivery does not implement replay endpoints or change the active OpenAPI.
- ReplaySession is distinct from Job and automatic Backtest Run. It freezes data/models, constrains all displayed data and screening to the session's historical time, and accepts manual order intents for subsequent-event matching through shared market/fill/cost/ledger components. Daily advancement is an atomic checkpoint transition with revision/idempotency/fencing protection; completed fills cannot be undone by navigating backward. Session end cancels remaining orders and preserves open positions instead of inventing a final-price liquidation.

## Operational notes

- Git is initialized at `c:\ssd\strategy` (work happens on branch `m0-foundation`); commit messages follow Conventional Commits and are written in English; PowerShell commits use `@'...'@` here-strings.
- When SQLite WAL is selected for local deployment, keep the database on a local filesystem, test checkpoint/busy/recovery behavior, and pin a SQLite runtime containing the current WAL-reset fix rather than trusting an arbitrary system version.
- Node toolchain quirks: vitest 5 writes its API token under `%LOCALAPPDATA%\vitest` first — `verify.ps1` redirects `LOCALAPPDATA` to `web/.local` for that step; Node's `Request` rejects relative URLs, so client tests use an absolute base URL while production keeps the same-origin default `/api/v1`.
