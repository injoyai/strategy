# 策略研究工作台设计方向

状态：需求阶段的设计种子，尚无运行时组件或样式。业务范围见 [需求说明](docs/requirements.md)，交互约定见 [界面需求](docs/frontend.md)。

## Overview

定位为长期使用的研究工具。主体是数据证据、实验对比和参数配置。视觉识别点是所有研究详情顶部的“研究上下文条”，以一致顺序显示快照、标的池、版本、时间范围，并允许定位依赖。

保持信息密度，避免营销式首屏、大面积装饰渐变、无含义的指标卡片。任务状态和研究可信度必须比装饰突出。

## Colors

初始浅色方案：工作区背景 #F3F6FA、内容表面 #FFFFFF、主文本 #172B4D、主操作蓝 #2457C5、辅助文本 #52627A、边界 #D6DEE9。成功 #18794E、警示 #8A5700、错误 #B42318；始终同时使用文本或图标表达状态。

净值主线用主操作蓝，基准用辅助灰；上涨/下跌显示正负号，市场色彩偏好以后可配置。颜色对比需在组件实现时验证，以上不是已经完成的可访问性审计。

## Typography

页面标题使用 Segoe UI、Microsoft YaHei、sans-serif，20–24px、600 字重；正文与表单使用相同中文友好栈，14px、1.6 行高；数据、版本和代码采用 Cascadia Mono、Consolas、monospace，数字启用 tabular-nums。标题克制，字段说明清楚。

## Layout

桌面左侧约 216px 导航，中间为主要研究区；参数/详情面板按可用宽度展开。采用 4/8/12/16/24px 间距序列。表格默认行高约 40px，提供更宽松密度。复杂表单自然纵向滚动，表格自身承担必要的横向滚动。

窄屏导航折叠、参数和结果顺序排列；上下文条换行，不以截断隐藏关键版本。图表预留高度，加载与错误不引发布局跳动。

## Elevation & Depth

内容区主要靠边界和间距区分。仅浮层、下拉菜单和模态框使用阴影。静态表格与图表不叠加多层卡片。

## Shapes

表单和按钮圆角 6px，面板 8px，状态标签小圆角；不将每个值渲染成胶囊。焦点框明显且不能被粘性栏遮挡。

## Components

计划共用：ResearchContextBar、PagedTable、SchemaForm、JobStatus、IssueList、MetricValue、ArtifactDownload。Ant Design 是基础控件的统一来源，ECharts 是图表统一来源。

未来样式所有权：`web/src/theme/tokens.ts` 作为实现时的统一 token 文件，再映射到 Ant Design theme、CSS variables 和图表主题；该路径目前是规划路径，不是现有文件。实现时同步本文与 tokens，禁止每个页面独立复制颜色。

下拉、日期、弹窗使用维护中的组件，并统一中文 locale；消息、加载、空态、确认框由共享封装提供。浏览器原生 alert/confirm 不作为工作台交互。

## Do's and Don'ts

- 保留数据单位、时间区间和版本；状态说明应能指导下一步操作。
- 不用颜色独自表达收益方向或错误；不以零代替不可得值。
- 不用前端计算替代后台指标定义，不让降采样改变报告结论。
- 页面尚未实现，视觉、主题映射、键盘和浏览器验证在开发阶段执行。
